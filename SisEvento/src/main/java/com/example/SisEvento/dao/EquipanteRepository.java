package com.example.SisEvento.dao;

import com.example.SisEvento.model.Equipante;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EquipanteRepository extends JpaRepository<Equipante, Long> {
}
